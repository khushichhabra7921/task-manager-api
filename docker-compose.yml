version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./tasks.db:/app/tasks.db
    environment:
      - SECRET_KEY=your-secret-key-change-in-production
    restart: unless-stopped
